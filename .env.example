# =============================
# Configurações de Ambiente
# =============================

# Ambiente de execução: development ou production
DJANGO_ENV=development

# =============================
# Configurações de Segurança
# =============================

# Chave secreta do Django
# - Em produção, use uma chave forte e segura.
# - Para gerar uma nova chave, execute no terminal:
#   python -c "import secrets; print(secrets.token_urlsafe())"
# - **IMPORTANTE:** Não compartilhe esta chave diretamente no arquivo .env com colegas.
#   Use meios seguros, como gerenciadores de senhas.
DJANGO_SECRET_KEY=django-insecure-000000

# Debug deve ser True apenas em ambiente de desenvolvimento
DEBUG=True

# Hosts permitidos
# - .vercel.app: Para deploy no Vercel (subdomínios).
# - seusite.com: Substitua pelo seu domínio personalizado (se configurado).
# - 127.0.0.1, localhost: Necessários para desenvolvimento local.
ALLOWED_HOSTS=127.0.0.1,localhost

# =============================
# Configurações de Banco de Dados
# =============================

# Nome do banco de dados (SQLite por padrão)
DB_NAME=db.sqlite3
